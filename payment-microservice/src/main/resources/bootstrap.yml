spring:
  config:
    import: "optional:configserver:"
  application:
     name: payment-microservice
  cloud:
        config:
            uri: http://localhost:8081
            username: root
            password: s3cr3t

  